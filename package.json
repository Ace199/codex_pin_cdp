{
  "name": "codex-chat-pin",
  "version": "0.5.6",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node scripts/pin-launcher.mjs --launch --watch",
    "launch": "node scripts/pin-launcher.mjs --launch --watch",
    "inject": "node scripts/pin-launcher.mjs --watch",
    "check": "node --check scripts/pin-launcher.mjs && node --check scripts/cdp-pipe.mjs && node --check inject/codex-pin.user.js"
  }
}
